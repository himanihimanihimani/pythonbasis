# a function is a container with a few code which performs a specification tasks
# a function is a block of code of code which runs only when it's called.
# Function is created when we have use same functionality multiple times.
def first_function():
    return "Hello"
result = first_function()
print(result)

def addition(num_1,num_2):#num1 and num2 are parameters
  return  num_1+num_2# adding the parameters
a = addition(num_1=2,num_2=3) 
print(a)